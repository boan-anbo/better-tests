import { loadScript } from "@src/index";

export const CommonScenesFixture = loadScript({
    story: "Common Scenes Fixture",
    scenes: {
        shouldWork: {
            story: "Common Scenes Should Work"
        }
    }
});

